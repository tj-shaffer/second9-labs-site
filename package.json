{
  "name": "second9-labs-site",
  "version": "0.1.0",
  "private": true,
  "description": "Second 9 Labs marketing site and Biba's Playground mini-apps. Static assets served by Cloudflare Workers.",
  "scripts": {
    "dev": "wrangler dev --port 8787 --persist-to /tmp/wrangler-state-bibas",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.92.0"
  },
  "dependencies": {
    "@cloudflare/puppeteer": "^1.1.0"
  }
}
